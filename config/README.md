# Environment configuration

Use `config/.env.example` as the documentation template. The test runner intentionally reads JVM properties and environment variables directly; this prevents test credentials from being loaded from an untracked local file or accidentally committed.
